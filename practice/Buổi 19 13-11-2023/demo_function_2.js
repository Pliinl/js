// Viết hàm giới thiệu bản thân
// Nhận vào thông tin và khóa học, sau đó output
// ra console

let name = "Plinl"; //get form input element
let course = "Javascript"; // get form input element

function greeting(name, course) {
  // tham số ~ parameter ~ param
  document.write(`My name is ${name}. My name is ${course}`);
}

greeting(name, course);
